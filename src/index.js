import ReactDOM from "react-dom";

function App () {
    return (
        <>
            <header>
                CINEFLEX
            </header>
        </>
    );
}

ReactDOM.render(<App />, document.querySelector(".root"));